import '../styling/hero.css';

const Hero = () => {
  return (
    <section className="hero-container">
      <h2 className="hero-title">Find Your Shoes</h2>
    </section>
  );
};

export default Hero;
